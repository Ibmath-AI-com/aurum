# ─── Remote Postgres ──────────────────────────────────────────────────────────
DATABASE_URL=postgres://USER:PASSWORD@15.135.254.123:5432/aurumdb
DB_HOST=15.135.254.123
DB_PORT=5432
DB_NAME=aurumdb
DB_USER=
DB_PASSWORD=
DB_MAX=10
DB_SSL=false

# ─── Local Redis (Docker) ─────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ─── API ─────────────────────────────────────────────────────────────────────
API_PORT=3000
NODE_ENV=development

# NOTE: Copy this file to .env and fill in values. Never commit .env.
# WARNING: migrations run against the LIVE remote aurumdb — never run
# destructive SQL casually. Always back up before a destructive migration.
